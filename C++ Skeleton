#include <bits/stdc++.h>

using namespace std;

#define for_loop_forward(iterator, size) for (int iterator = 0; iterator < size; iterator++)
#define for_loop_backward(iterator,size) for (int iterator = size - 1; iterator >= 0; iterator--)

typedef unsigned long long int bigint; //big int

/*
--------------------------------
    declaring dynamic  array:
    int* ary = new int[size];
--------------------------------------
 declaring dynamic 2d array:
    int **ary = new int*[sizeY];
    for (int i = 0; i < sizeY; i++) {
    ary[i] = new int[sizeX];


    deleting memory to prevent memory leak
    for (int i = 0; i < length; ++i) {
        delete[] grid[i];
    }
    delete[] grid;
-------------------------------------------
 */

int main()
{
  // Code
    return 0;
}
